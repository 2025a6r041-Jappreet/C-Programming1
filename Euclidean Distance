#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float x1,x2,y1,y2,d;
    scanf("%f%f%f%f",&x1,&x2,&y1,&y2);
    d=sqrt(pow((x1-y1),2)+pow((x2-y2),2));
    printf("%.2f",d);
    return 0;
}
