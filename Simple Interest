#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int p;
    float r,t,s;
    scanf("%d",&p);
    scanf("%f%f",&r,&t);
    s=(p*r*t)/100;
    printf("%.2f",s);
    
    return 0;
}
