#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

void fibonaaci(int n){
     static int a=0, b=1, c, i=0;
     if(i<n){
        if(i == 0)
            printf("0 ");
        else if (i == 1)
            printf("1 ");
        else{
            c = a + b;
            printf("%d ",c);
            a = b;
            b = c;
        }
        i++;
        fibonaaci(n);
    }
}
int main(){
    int num;
    scanf("%d",&num);
    fibonaaci(num);
    return 0;
}
