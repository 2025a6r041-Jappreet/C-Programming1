#include <stdio.h>
int main()
{
    int num, sum = 0, digit;
    num = 446;

    while (num != 0)
    {
        digit = num % 10; 
        sum += digit;     
        num = num / 10;   
    }
    printf("Sum of the digits: %d\n", sum);
    return 0;
}
